paths:
  api/carts/:
    get:
      summary: Gets a list of carts
      tags:
        - Carts
      responses:
        "200":
          description: List of carts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Cart"
